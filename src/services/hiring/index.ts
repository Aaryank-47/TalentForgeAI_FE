/**
 * Hiring domain service layer — replace mock implementations with API calls in Phase 2+.
 * Pages and context should consume these services rather than calling fetch directly.
 */

export * from '../types/hiring';

// Future: workflowService, interviewTemplateService, questionLibraryService
